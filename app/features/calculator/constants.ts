// 잔금대출 비율(%)의 기본값 (예: 60%)
export const BALANCE_LOAN_RATIO = 60;

// 잔금대출 비율(%)의 최대 허용값 (예: 80%)
export const BALANCE_LOAN_RATIO_MAX = 80;

// 잔금대출 이자율(%)의 기본값 (예: 4.5%)
export const BALANCELOAN_RATE = 4.5;

// 잔금대출 이자율(%)의 최대 허용값 (예: 7.0%)
export const BALANCELOAN_RATE_MAX = 7.0;

// 잔금대출 이자율(%)의 최소 허용값 (예: 2.0%)
export const BALANCELOAN_RATE_MIN = 2.0;

// 계약금 비율(%)의 기본값 (예: 10%)
export const DEPOSIT_RATIO = 10;

// 계약금 비율(%)의 최대 허용값 (예: 30%)
export const DEPOSIT_RATIO_MAX = 30;

// 계약금 비율(%)의 최소 허용값 (예: 5%)
export const DEPOSIT_RATIO_MIN = 5;

// 공실(빈방) 개월수의 기본값 (예: 6개월)
export const EMPTY_MONTH = 6;

// 공실 개월수의 최대 허용값 (예: 24개월)
export const EMPTY_MONTH_MAX = 24;

// 공실 개월수의 최소 허용값 (예: 2개월)
export const EMPTY_MONTH_MIN = 2;

// 중도금 대출 비율(%)의 기본값 (예: 40%)
export const MIDDLE_LOAN_RATIO = 40;

// 중도금 대출 이자율(%)의 기본값 (예: 6%)
export const MIDDLELOAN_RATE = 6;

// 중도금 대출 이자율(%)의 최대 허용값 (예: 10.0%)
export const MIDDLELOAN_RATE_MAX = 10;

// 중도금 대출 이자율(%)의 최소 허용값 (예: 3.0%)
export const MIDDLELOAN_RATE_MIN = 3.0;

// 취득세율(%) (예: 5% - 등기비 포함)
export const ACQUISITION_TAX_RATIO = 5;

// 평(3.3㎡)당 관리비(원) 기본값 (예: 10,000원)
export const PER_PY = 10000;

// 평당 최대 분양가(원) (예: 1억원)
export const PRICE_PY_MAX = 100000000;

// 평당 최소 분양가(원) (예: 500만원)
export const PRICE_PY_MIN = 5000000;

// 분양가 최소 허용값(원) (예: 1억원)
export const SALE_PRICE_MIN = 100000000;
// 필요한 추가 상수는 여기에 계속 추가
